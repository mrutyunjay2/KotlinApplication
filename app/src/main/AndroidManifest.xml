<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mtj.example.kotlinapplication">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinApplication">
        <activity android:name="mtj.example.RXJavaConcept.RxSubjectActivity"></activity>
        <activity android:name="mtj.example.RXJavaConcept.skipFilterConatActivity" />
        <activity android:name="mtj.example.RXJavaConcept.RXActivity" />
        <activity android:name="mtj.example.dagger2.model.DaggerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>